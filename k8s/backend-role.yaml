apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: backend-gateway-restart
  namespace: ibkr
rules:
  - apiGroups: ["apps"]
    resources: ["deployments"]
    verbs: ["get", "patch"]
